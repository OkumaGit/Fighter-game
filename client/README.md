# Arena Clash

## 🏃‍♂️ Simple start

1. **`npm install`** at the root
2. **`npm run dev`** at the root
3. open **`http://localhost:7800/`**

## ℹ️ PS

The project includes local assets and an original fighter roster instead of licensed characters.
